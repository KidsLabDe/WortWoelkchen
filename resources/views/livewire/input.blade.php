<div>
    {{-- The best athlete wants his opponent at his best. --}}
    <h1 class="display-5 fw-bold text-body-emphasis lh-1 mb-3">{{ $survey->question }}</h1>
    <p class="lead">{{ $survey->id }}</p>
    <p class="lead">{{ $survey->start }}</p>
</div>
